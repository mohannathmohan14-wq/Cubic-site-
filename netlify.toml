[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Route every /api/* call to the single backend function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
